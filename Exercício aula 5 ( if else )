#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	/*
	//Exemplo 
	
    int a, b, c, r;
	
	a = 10;
	b = 1;
	c = 0;
	
	r = a != b;
	
	printf ("%d", r);
	*/
	
	/*
	//Exercicio 10 lista 2, feito de outro jeito
	
	int a, b, c, r;
	
	printf("Entre com os valores para A B C: " );
	scanf("%d %d %d", &a, &b, &c);
	
	if (a>b) {
	    r = a;
	}
	if (b>a) {
		r = b;
	}
	if (c>r) {
		r = c;
	}
	//Não é necessario colocar o r>c, e não devemos colocar, fazer o código com o minimo de comparações possiveis
	
	printf ("%d he o maior ", r);
	*/
	
	/*
	//Outro jeito, para diminuir ainda mais o código 
	int a, b, c, r;
	
	printf("Entre com os valores para A B C: " );
	scanf("%d %d %d", &a, &b, &c);
	
	if (a>b) {
	    r = a;
	}
	else{
		r = b;
	}
	if (c>r) {
		r = c;
    }
	printf ("%d he o maior ", r);
	*/
	
	//Ler um valor N e falar se é par ou impar
	
	int n;
	
	printf ("Digite um numero: ");
	scanf("%d", &n);
	
	if (n % 2 == 0){
	    printf("Par");
    }
    else{
    	printf("Impar");
	}
	
	
	return 0;
}
