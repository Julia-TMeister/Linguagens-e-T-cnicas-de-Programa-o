 #include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {

   /*
   //exercicio 10-lista 2 feito pelo prof
   
   int a,b,c, maior, maior_tempo;
   printf("Informe os valores a serem comparados: ");
   scanf("%d %d %d", &a,&b,&c);
   
   //conta
   maior_tempo = (a+b+abs(a-b))/2;
   
   maior = (maior_tempo+c+abs(maior_tempo-c))/2;
   
   printf("O maior entre |%d|%d|%d| = %d", a, b, c, maior); 
   */
   
   /*
   // ex extra: agora com 4 valores
   int a,b,c,d, maior, maior_tempo1, maior_tempo2;
   printf("Informe os valores a serem comparados: ");
   scanf("%d %d %d %d", &a, &b, &c, &d);
   
   maior_tempo1 = (a+b+abs(a-b))/2;
   maior_tempo2 = (maior_tempo1+c+abs(maior_tempo1-c))/2;
   maior = (maior_tempo2+d+abs(maior_tempo2-d))/2;
   
   printf("O maior entre |%d|%d|%d|%d| = %d", a, b, c, d, maior);
   */
   
	return 0;
}
